<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="wizard.fems.energyMon.alarm.alarmMapper">

    <select id="getCustom" resultType="hashmap">
        select  customID
             ,  kCustom
        from mt_custom
        where businessTypeCode = '02'
    </select>

    <select id="getMachine" resultType="hashmap">
        select  mcID
             ,  mcName
        from mt_mc

    </select>

    <select id="getAlarmList" statementType="CALLABLE" resultType="wizard.fems.energyMon.alarm.DTO.alarmDTO"
            parameterType="map">
        { call getAlarmList(#{mcID, javaType=String, jdbcType=VARCHAR, mode=IN},
                            #{sDate, javaType=String, jdbcType=VARCHAR, mode=IN},
                            #{eDate, javaType=String, jdbcType=VARCHAR, mode=IN},
                            #{alarmLevel, javaType=Integer, jdbcType=SMALLINT, mode=IN}
               )}
    </select>

</mapper>